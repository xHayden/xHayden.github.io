<div class="page-sizing py-4">
    <nav class="flex w-full justify-start py-2">
        {% for item in collections.sortedNavItems %}
            <a href="{{ item.url | urlPrefix | url }}">{{ item.data.title }}</a>
        {% endfor %}
        <a href="/" class="ml-auto">Portfolio -></a>
    </nav>
</div>